# multi-pass-generator
A password generator that creates multiple password sets. Configure different blocks with unique settings (length, character sets including Latin/Cyrillic/Armenic alphabets), generate hundreds of secure passwords at once, and export them to organized folders. Features dark/light themes, bilingual interface (RU/EN), and standalone EXE build support.
